#include <iostream>
#include <vector>

using namespace std;

int main() {
    int N;  // number of pieces of glass
    scanf("%d", &N);
    
    int T;  // tint threshold
    scanf("%d", &T);
    
    for (int i = 0; i < N; i++) {
        int xl, yt, xr, yb, ti;
        scanf("%d %d %d %d %d", &xl, &yt, &xr, &yb, &ti);
        
        
    }
    
    return 0;
}
